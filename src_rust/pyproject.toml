[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "phantom_core"
version = "0.1.0"
description = "High-performance Rust core for PhantomArbiter"
requires-python = ">=3.12"

[tool.maturin]
features = ["pyo3/extension-module"]
