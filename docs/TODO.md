# Tactical TODO (Next 48h)

**Context**: Stabilizing the Hybrid Architecture.

## 🚨 Immediate Tasks
1.  [ ] **Verify Galaxy Launch**: Ensure `main.py dashboard` launches `apps/galaxy` correctly.
2.  [ ] **Test Bridges**: Run `bridges/orca_daemon.js` manually to check connectivity.
3.  [ ] **Fix Scraper**: Resolve `ImportError` in `src/scraper` (blocking Ingestion).
4.  [ ] **Ghost Run**: Execute `scripts/ghost_execute.py` to validate Jito formatting.

*(For long-term goals, see [ROADMAP.md](./ROADMAP.md))*
